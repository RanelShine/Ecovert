{
  "build": {
    "env": {
      "PYTHONUNBUFFERED": "1"
    },
    "command": "cd backend && pip install -r requirements.txt"
  },
  "start": {
    "command": "cd backend && gunicorn backend.wsgi --log-file -"
  }
}
